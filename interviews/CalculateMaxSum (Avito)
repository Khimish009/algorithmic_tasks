Дан массив положительных целых чисел (длина N), описывающий цену единицы товара на протяжении N дней. Мы каждый день производим по одной единице товара. У нас есть склад, где мы можем хранить свой товар. Требуется вычислить максимальную сумму, которую мы можем выручить за произведённые нами товары, с учётом, что к концу периода все товары должны быть проданы.

// Привер:
// [1, 3, 1, 2] => 10

const prices = [1, 3, 1, 2]

function calculateMaxSum(prices) {
  let prevMax = sum = 0

  for (let i = prices.length - 1; i >= 0; i--) {
    if (prices[i] > prevMax) {
      prevMax = prices[i]
    }

    sum += prevMax
  }

  return sum
}

console.log(calculateMaxSum(prices))
