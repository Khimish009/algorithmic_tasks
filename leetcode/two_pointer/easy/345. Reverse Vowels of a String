https://leetcode.com/problems/reverse-vowels-of-a-string/description/?envType=study-plan-v2&envId=leetcode-75

Given a string s, reverse only all the vowels in the string and return it. The vowels are 
'a', 'e', 'i', 'o', and 'u', and they can appear in both lower and upper cases, more than once.

Example 1:
Input: s = "hello"
Output: "holle"

Example 2:
Input: s = "leetcode"
Output: "leotcede"

Constraints:
1 <= s.length <= 3 * 105
s consist of printable ASCII characters.

Solution:
var reverseVowels = function(s) {
    const vowels = s.match(/[aeiou]/gi)
    return s.replace(/[aeiou]/gi, () => vowels.pop())
}

My first solution:
var reverseVowels = function(s, str = s.split('')) {
    let left = 0
    let right = str.length - 1

    while(left < right) {
        const isVowelsLeft = isVowels(str[left])
        const isVowelsRight = isVowels(str[right])
        if(isVowelsLeft && isVowelsRight) {
            [str[left], str[right]] = [str[right], str[left]]
            left++
            right--
        }

        if (!isVowelsLeft) left++
        if (!isVowelsRight) right--
    }

    return str.join('')
};

function isVowels(letter) {
    return ['a', 'e', 'i', 'o', 'u'].includes(letter.toLowerCase())
}
