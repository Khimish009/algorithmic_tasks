Solution:
    Time complexity O(n)
    Space complexity O(n)

var distributeCandies = function(candyType) {
    const maxAllowedToEat = Math.floor(candyType.length / 2)
    const candySet = new Set(candyType)
    
    return Math.min(candySet.size, maxAllowedToEat)
};
